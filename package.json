{
  "name": "tomato-clock",
  "version": "1.0.0",
  "description": "A Pomodoro Timer app with a to-do list, built with Tailwind CSS and vanilla JavaScript.",
  "main": "script.js",
  "scripts": {
    "build": "tailwindcss -i ./style.css -o ./public/style.css --minify && copy index.html public\\index.html && copy script.js public\\script.js",
    "dev": "npx tailwindcss -i ./style.css -o ./public/style.css --watch",
    "start": "serve public"
  },
  "keywords": [
    "pomodoro",
    "tailwind-css",
    "javascript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serve": "^14.2.4",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
